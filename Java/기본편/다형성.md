# 다형성 (Polymorphism) 1

## 핵심 개념
다형성은 **하나의 변수가 여러 타입의 객체를 참조할 수 있는 능력**이다.
다형성은 두 가지 핵심 이론으로 구성된다: **다형적 참조**와 **메서드 오버라이딩**.
다형적 참조 + 메서드 오버라이딩이 함께 작동할 때 진정한 다형성의 힘이 발휘된다.

---

## 1. 다형적 참조 (Polymorphic Reference)
부모 타입의 변수가 자식 인스턴스를 참조할 수 있는 것이다.
**부모는 자식을 담을 수 있지만, 자식은 부모를 담을 수 없다.**

```java
Parent poly = new Child();   // ✅ 부모 변수 → 자식 인스턴스 (다형적 참조)
poly.parentMethod();         // ✅ 부모 타입에 있는 메서드만 호출 가능
// poly.childMethod();       // ❌ 컴파일 오류 - 자식 메서드 호출 불가

Child child = new Parent();  // ❌ 컴파일 오류 - 자식은 부모를 담을 수 없음
```

**다형적 참조의 한계:** 변수 타입이 Parent이면 Parent 클래스에서 위로만 탐색한다.
자식 방향으로 내려갈 수 없으므로 자식 메서드를 호출하려면 **다운캐스팅**이 필요하다.

---

## 2. 캐스팅 (Casting)

| 종류 | 방향 | 생략 가능 여부 | 안전 여부 |
|------|------|--------------|----------|
| 업캐스팅 | 자식 → 부모 | ✅ 생략 권장 | ✅ 항상 안전 |
| 다운캐스팅 | 부모 → 자식 | ❌ 명시적 필수 | ⚠️ 잘못하면 RuntimeException |

```java
// 업캐스팅 - 생략 가능 (자동)
Parent parent = child;              // (Parent) child 와 동일

// 다운캐스팅 - 반드시 명시적
Child child = (Child) poly;         // 변수에 저장
((Child) poly).childMethod();       // 일시적 다운캐스팅 (변수 불필요)
```

**다운캐스팅이 위험한 이유:**
`new Parent()`로 생성한 객체는 메모리에 Child가 존재하지 않는다.
이 상태에서 `(Child)` 캐스팅하면 **ClassCastException** (런타임 오류)이 발생한다.

---

## 3. instanceof
다운캐스팅 전에 반드시 실제 인스턴스 타입을 확인하는 키워드이다.
오른쪽 타입에 왼쪽 인스턴스가 **대입 가능하면 true**, 불가능하면 false를 반환한다.

```java
// Java 16+ Pattern Matching (권장)
if (parent instanceof Child child) {  // 확인 + 다운캐스팅을 한 줄로
    child.childMethod();
}
```

---

## 4. 메서드 오버라이딩과 다형성
**오버라이딩된 메서드는 항상 우선권을 가진다.**
변수 타입이 Parent이어도, 실제 인스턴스가 Child이고 메서드가 오버라이딩되었으면 Child의 메서드가 실행된다.
**단, 멤버 변수는 오버라이딩되지 않는다** — 변수 타입 기준으로 접근한다.

```java
Parent poly = new Child();
poly.method();   // → Child.method() 실행 (오버라이딩 우선권)
poly.value;      // → "parent" 출력 (변수는 오버라이딩 안됨, Parent 타입 기준)
```

---

## 면접 대비 핵심

**Q1. 다형적 참조가 왜 가능한가?**
- 자식은 부모의 모든 기능을 상속받아 가지고 있기 때문이다.
- `new Child()`를 생성하면 메모리에 Parent와 Child가 모두 생성되므로, Parent 타입 변수로 참조해도 Parent 부분은 반드시 존재한다.

**Q2. 업캐스팅은 안전하고 다운캐스팅은 위험한 이유?**
- 객체 생성 시 상위 부모는 항상 함께 생성되지만, 하위 자식은 생성되지 않는다.
- 업캐스팅은 반드시 존재하는 타입으로 이동하므로 안전하고, 다운캐스팅은 존재하지 않을 수 있는 타입으로 이동하므로 위험하다.

**Q3. 다운캐스팅을 명시적으로 해야하는 이유?**
- 잘못된 다운캐스팅은 ClassCastException 런타임 오류를 발생시킨다.
- 개발자가 "이 객체가 실제로 이 타입인지" 직접 인식하고 책임을 가져야 하는 의미로 명시적 캐스팅을 강제한다.

**Q4. instanceof와 다운캐스팅의 관계?**
- instanceof로 먼저 실제 타입을 확인한 후에만 다운캐스팅을 수행해야 ClassCastException을 방지할 수 있다.
- Java 16+에서는 `instanceof Child child` 패턴 매칭으로 확인과 캐스팅을 한 줄로 처리할 수 있다.

**Q5. 오버라이딩된 메서드와 멤버 변수의 접근 차이?**
- 메서드는 실제 인스턴스의 타입 기준으로 실행된다 (오버라이딩 우선권).
- 멤버 변수는 참조 변수의 선언 타입 기준으로 접근된다 (오버라이딩 불가).
- `Parent poly = new Child()`일 때, `poly.method()`는 Child 버전이지만 `poly.value`는 Parent 버전이다.

# 11. 다형성2 — 다형성 활용 · 추상 클래스 · 인터페이스

---

## 1. 다형성 활용: 왜 필요한가

다형성 없이 Dog, Cat, Caw를 각각 직접 사용하면 동물이 추가될 때마다 동일한 구조의 코드가 중복됩니다.
중복을 제거하려면 메서드나 배열을 사용해야 하는데, 타입이 서로 다르면 하나의 매개변수·배열에 넣을 수 없습니다.
**문제의 핵심 → 타입이 다르다.** 같은 타입으로 만들면 중복이 사라진다.

### 다형성으로 중복 제거 (상속 활용)
```java
// 부모 클래스 — 오버라이딩 대상 메서드 제공
public class Animal {
    public void sound() {
        System.out.println("동물 울음 소리");
    }
}

// 자식 클래스 — 각각 오버라이딩
public class Dog extends Animal {
    @Override
    public void sound() { System.out.println("멍멍"); }
}
```
```java
// ⭐ 핵심 코드 — 동물이 추가되어도 변하지 않는 부분
private static void soundAnimal(Animal animal) {  // 다형적 참조
    System.out.println("동물 소리 테스트 시작");
    animal.sound();  // 메서드 오버라이딩 → 실제 자식의 sound() 호출
    System.out.println("동물 소리 테스트 종료");
}
```

**동작 원리:**
1. `soundAnimal(dog)` 호출 → `Animal animal = dog` (부모는 자식을 담을 수 있음)
2. `animal.sound()` 호출 시 변수 타입은 `Animal`이지만, 실제 인스턴스가 `Dog`
3. Dog에서 `sound()`를 오버라이딩했으므로 **오버라이딩한 메서드가 우선권** → `Dog.sound()` 실행

**배열 + for문으로도 가능:**
```java
Animal[] animalArr = {new Dog(), new Cat(), new Caw()};
for (Animal animal : animalArr) {
    soundAnimal(animal);  // 각 인스턴스의 오버라이딩된 메서드 호출
}
```

> 💡 변하는 부분(main — 객체 생성)과 변하지 않는 부분(soundAnimal)을 분리하는 것이 핵심

---

## 2. 남은 문제 → 추상 클래스로 해결

일반 클래스 `Animal`에는 두 가지 위험이 있습니다:
- `new Animal()`로 직접 인스턴스를 생성할 수 있음 (의미 없는 객체)
- 자식이 실수로 `sound()`를 오버라이딩하지 않을 수 있음 (컴파일 오류 없이 통과)

### 추상 클래스 + 추상 메서드
```java
public abstract class AbstractAnimal {       // abstract → 직접 new 불가
    public abstract void sound();            // abstract → 자식이 반드시 오버라이딩 필수
    public void move() {                     // 일반 메서드 → 오버라이딩 선택적
        System.out.println("동물이 움직입니다.");
    }
}
```

| 키워드 | 역할 |
|--------|------|
| `abstract class` | 직접 인스턴스 생성 불가. 상속 목적으로만 사용 |
| `abstract void sound()` | 바디 없음. 자식이 반드시 구현해야 함. 미구현 시 컴파일 오류 |
| 일반 메서드 `move()` | 자식이 구현하지 않아도 상속받아 사용 가능 |

> 추상 클래스 = 제약이 추가된 클래스. 메모리 구조·실행 결과는 일반 클래스와 동일

---

## 3. 순수 추상 클래스 → 인터페이스로 진화

모든 메서드가 `abstract`인 추상 클래스를 **순수 추상 클래스**라 합니다.
이것은 실행 로직이 전혀 없고, 다형성을 위한 **규격(규약)만** 정의합니다.

자바는 이 개념을 더 편리하게 사용할 수 있도록 **인터페이스**를 제공합니다.
```java
// 순수 추상 클래스
public abstract class AbstractAnimal {
    public abstract void sound();
    public abstract void move();
}

// 인터페이스 — public abstract 생략 가능 (권장)
public interface Animal {
    void sound();
    void move();
}
```

**인터페이스의 추가 장점:**
- **제약 강화:** 미래에 누군가 실행 가능한 메서드를 끼워넣을 수 없음 (모두 abstract)
- **다중 구현 가능:** `implements A, B` → 클래스 상속은 하나만 가능하지만 인터페이스는 여러 개

### 구현 코드
```java
public class Dog implements Animal {          // implements (구현) ← extends (상속) 아님
    @Override
    public void sound() { System.out.println("멍멍"); }
    @Override
    public void move()  { System.out.println("개 이동"); }
}
```

---

## 4. 인터페이스 다중 구현 & 다이아몬드 문제

클래스 다중 상속은 불가 → **다이아몬드 문제** (어떤 부모의 메서드를 호출할지 모호)

인터페이스는 다중 구현 가능합니다. 이유: 인터페이스는 구현체가 없으므로, 자식이 직접 구현하면 모호성이 없습니다.
```java
public class Child implements InterfaceA, InterfaceB {
    @Override
    public void methodA()      { System.out.println("Child.methodA"); }
    @Override
    public void methodB()      { System.out.println("Child.methodB"); }
    @Override
    public void methodCommon() { System.out.println("Child.methodCommon"); } // 하나만 구현하면 됨
}
```

---

## 5. 클래스 상속 + 인터페이스 구현 혼용

추상 클래스(상속)와 인터페이스(구현)를 함께 사용하는 패턴입니다.
```java
// 추상 클래스 — 공통 기능(move) + 추상 메서드(sound)
public abstract class AbstractAnimal {
    public abstract void sound();
    public void move() { System.out.println("동물이 이동합니다."); }
}

// 인터페이스 — 특정 능력의 규격 정의
public interface Fly {
    void fly();
}

// Bird — 클래스 상속 + 인터페이스 구현 (extends가 먼저)
public class Bird extends AbstractAnimal implements Fly {
    @Override
    public void sound() { System.out.println("짹짹"); }
    @Override
    public void fly()   { System.out.println("새 날기"); }
}
```

> `extends`는 하나만 가능 → `implements`는 여러 개 가능. 함께 쓰면 `extends`가 먼저 옴

---

## 면접 대비 핵심

**Q1. 다형성의 두 가지 핵심 요소는?**
- **다형적 참조:** 부모 타입 변수가 자식 인스턴스를 참조할 수 있음 (`Animal a = new Dog()`)
- **메서드 오버라이딩:** 부모 타입으로 호출해도 실제 자식의 메서드가 실행됨 (오버라이딩 우선권)

**Q2. 추상 클래스와 인터페이스의 차이?**
- 추상 클래스: 일반 메서드 포함 가능 → 공통 구현을 상속시킬 수 있음. 상속 하나만 가능
- 인터페이스: 모든 메서드가 abstract (규격만 정의). 다중 구현 가능. `implements` 사용

**Q3. 인터페이스를 순수 추상 클래스 대신 사용하는 이유?**
- 제약 강화: 누구도 구현 메서드를 끼워넣을 수 없음
- 다중 구현 지원: 클래스 상속은 단일만 가능하지만 인터페이스는 여러 개 가능
- 설계의 명확성: "이 클래스는 이 규격을 守한다"는 의도가 명확

**Q4. 다이아몬드 문제가 인터페이스에서는 발생하지 않는 이유?**
- 인터페이스는 구현체(바디)가 없음. 구현은 자식이 직접 하므로 모호성이 없음

**Q5. extends와 implements를 함께 사용할 때의 규칙?**
- `extends`는 클래스 하나만 가능, `implements`는 여러 개 가능
- 함께 쓰면 반드시 `extends`가 먼저: `class Bird extends Animal implements Fly, Swim`

# 12. 다형성과 설계

## 핵심 개념

**다형성(Polymorphism)** : 하나의 역할(인터페이스)에 대해 여러 구현(클래스)이 존재할 수 있는 것.
클라이언트는 역할(인터페이스)만 알면 되고, 구현이 바뀌어도 클라이언트 코드는 변경 불필요.
실세계 비유: 운전자는 "자동차"라는 역할만 알면 되고, K3든 테슬라든 신경 안 씨 된다.

**역할과 구현 분리:**
- 역할 = 인터페이스
- 구현 = 인터페이스를 구현한 클래스

---

## 주요 문법/특징

### 다형성 없이 구현한 경우 (문제점)

차량이 추가될 때마다 Driver 내부 코드가 변경되어야 한다.
```java
// ❌ 다형성 미적용 - 차량 추가마다 Driver가 변경됨
public class Driver {
    private K3Car k3Car;
    private Model3Car model3Car; // 차량 추가마다 필드 추가

    public void drive() {
        if (k3Car != null) {           // 분기 로직이 계속 증가
            k3Car.startEngine();
        } else if (model3Car != null) {
            model3Car.startEngine();
        }
    }
}
```

### 다형성을 적용한 경우 (해결)
```java
// ✅ 다형성 적용 - Driver는 Car 인터페이스만 의존
interface Car {
    void startEngine();
    void offEngine();
    void pressAccelerator();
}

class K3Car implements Car {
    @Override
    public void startEngine() { System.out.println("K3Car.startEngine"); }
    // offEngine(), pressAccelerator() 구현...
}

class Model3Car implements Car {
    @Override
    public void startEngine() { System.out.println("Model3Car.startEngine"); }
    // offEngine(), pressAccelerator() 구현...
}

class Driver {
    private Car car; // 인터페이스만 참조 → 구현체 교체 자유

    public void setCar(Car car) { this.car = car; }

    public void drive() {
        car.startEngine();         // 오버라이딩에 의해 실제 구현체의 메서드 호출
        car.pressAccelerator();
        car.offEngine();
    }
}

// 사용 시점: 런타임에 구현체를 교체할 수 있다
Car k3 = new K3Car();
driver.setCar(k3);
driver.drive(); // → K3Car의 메서드 실행

Car model3 = new Model3Car();
driver.setCar(model3);
driver.drive(); // → Model3Car의 메서드 실행 (Driver 코드 변경 없음)
```

---

### OCP (Open-Closed Principle)

다형성을 잘 적용하면 OCP가 자동으로 지켜진다.
- **Open for extension** : Car 인터페이스를 구현하여 새 차량을 자유롭게 추가 가능
- **Closed for modification** : Driver(클라이언트) 코드는 전혀 변경 불필요
- 변경되는 부분: main() 등 객체를 생성하고 조율하는 부분만

### 전략 패턴 (Strategy Pattern)

위의 Car/Driver 구조가 곧 전략 패턴이다.
- 전략 정의 = Car 인터페이스
- 구체적 전략 = K3Car, Model3Car
- 전략을 사용하는 컨텍스트 = Driver
- 클라이언트 코드 변경 없이 알고리즘(구현)을 교체할 수 있다.

---

## 면접 대비 핵심

**Q1. 다형성이란 무엇인가?**
- 하나의 인터페이스(역할)에 대해 여러 구현 클래스가 존재할 수 있고, 실행 시점에 구현체를 유연하게 교체할 수 있는 것.
- JVM은 실제 객체의 타입을 기반으로 오버라이딩된 메서드를 호출한다 (동적 디스패치).

**Q2. OCP(열린/닫힌 원칙)를 코드로 설명할 수 있나?**
- 인터페이스로 역할을 정의하고, 클라이언트가 인터페이스만 의존하면 새로운 구현을 추가해도 클라이언트 코드 수정이 불필요하다.
- 예: Pay 인터페이스 → KakaoPay, NaverPay 추가 시 PayService 변경 없음.

**Q3. 역할과 구현을 분리하는 이유?**
- 클라이언트가 구현의 내부 구조를 몰라도 된다.
- 구현 대상 자체를 교체하여도 클라이언트에 영향 없다.
- 유연하고, 확장 가능한 설계를 할 수 있다.

**Q4. 전략 패턴과 다형성의 관계?**
- 전략 패턴은 다형성을 활용한 디자인 패턴이다.
- 인터페이스를 통해 알고리즘(구현)을 클라이언트 변경 없이 교체하는 것이 전략 패턴의 본질.
- Spring의 IoC/DI도 결국 다형성과 전략 패턴의 원리로 동작한다.

**Q5. 결제 시스템에서 DefaultPay를 사용하는 이유?**
- 결제 수단을 찾지 못했을 때 null을 반환하면 클라이언트에서 null 체크가 필요하다.
- DefaultPay(항상 실패하는 구현체)를 제공하면 null 체크 없이 균일하게 처리할 수 있다.
- 이것은 Null Object Pattern이라는 패턴이다.
